[project]
name = "experiment-with-genomics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=3.0.0",
    "lightning>=2.6.1",
    "wandb>=0.19.0",
    "modal>=1.4.3",
    "torch==2.8.0",
    # Nucleotide Transformer Hub code imports find_pruneable_heads_and_indices from
    # transformers.pytorch_utils; that symbol was removed in transformers 5.8+.
    "transformers>=4.44,<5",
    "tqdm>=4.67.3",
    "matplotlib>=3.10.9",
    "fastapi>=0.136.3",
]
